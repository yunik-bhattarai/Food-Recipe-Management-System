<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Recipe Management{% endblock %}</title>
    <link rel="stylesheet" href="assets/css/style.css?v={{ "now"|date('U') }}">
    
    <style>
        /* Your existing styles remain here... */
        html, body { margin: 0; padding: 0; width: 100% !important; }
        header { width: 100% !important; background-color: #2c3e50 !important; display: flex !important; flex-direction: column !important; align-items: center !important; padding: 0 !important; }
        header h1 { color: white !important; margin: 0 !important; padding: 20px 0 !important; text-align: center !important; width: 100% !important; }
        nav { width: 100% !important; background-color: #34495e !important; display: flex !important; justify-content: flex-end !important; padding: 12px 0 !important; margin: 0 !important; }
        .nav-content { display: flex !important; width: auto !important; max-width: none !important; margin: 0 !important; padding-right: 50px !important; }
        nav a { color: white !important; text-decoration: none !important; margin-left: 30px !important; font-weight: 500 !important; }
        main { width: 100% !important; max-width: 1100px !important; margin: 0 auto !important; padding: 40px 20px !important; box-sizing: border-box !important; }

        /* ADD THIS CSS to make your suggestions visible */
        .suggestions {
            position: absolute;
            background: white;
            border: 1px solid #ccc;
            width: 100%;
            z-index: 1000;
            color: #333;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>

    <header>
        <h1>Food Recipe Management</h1>
        <nav>
            <div class="nav-content">
                <a href="index.php">Home</a>
                <a href="add.php">Add Recipe</a>
                <a href="search.php">Search</a>
                {% if session_user %}
                    <a href="logout.php">Logout ({{ session_user }})</a>
                {% else %}
                    <a href="login.php">Login</a>
                {% endif %}
            </div>
        </nav>
    </header>

    <main>
        {% block content %}{% endblock %}
    </main>

    <footer style="width:100%; background:#2c3e50; color:white; text-align:center; padding:20px 0; margin-top:auto;">
        <p>&copy; 2026 Food Recipe Management - YB</p>
    </footer>

    <script src="assets/js/script.js?v={{ "now"|date('U') }}"></script>
</body>
</html>